site_name: Wiki 安裝工程
site_url: https://github.com/drogan-gas/KK/ # 💡 務必加上這一行！結尾要有斜線 /
theme:
  name: material
  # 💡 調整為 Minimal 質感核心設定
  palette:
    scheme: slate       # 強制使用深色模式
    primary: custom     # 自訂主色調
    accent: custom      # 自訂強調色
  features:
    - content.code.copy # 讓程式碼區塊右上有複製按鈕

plugins:
  - search
  - roamlinks # 支援 Obsidian 的 [[雙向連結]] 語法

docs_dir: 'WebWiki' # 💡 這裡必須與你實際要公開的資料夾名稱完全一致

# 💡 告訴 MkDocs 要載入這個額外的 CSS 檔案
extra_css:
  - stylesheets/extra.css
  
# 💡 1. 啟用 Markdown 擴充功能以支援 Python Markdown 中的 SuperFences
markdown_extensions:
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format

# 💡 2. 載入 Material 主題內建的 Mermaid 渲染腳本
extra_javascript:
  - https://unpkg.com/mermaid@10/dist/mermaid.min.js